#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int T; cin >> T;

    int x, y, z, k;
    while(T--){
        cin >> x >> y >> z >>k;
        cout << 0 << endl;
    }

    return 0;
}