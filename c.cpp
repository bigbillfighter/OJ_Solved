#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    // int left = 0, right = n-1;
    // while(left < right){
    //     while(a[left] + a[right] > k ){
    //         if((a[left + 1] - a[left]) > (a[right] - a[right-1])){
    //             right --;
    //         }else{
    //             left++;
    //         }
    //     }
    //     if(a[left]+a[right] == k) rst.push_back(make_pair(left, right))
    //     else{
    //         if((a[left + 1] - a[left]) > (a[right] - a[right-1])){
    //             right --;
    //         }else{
    //             left++;
    //         }
    //     }
    // }


    return 0;
}